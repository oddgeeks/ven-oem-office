export enum PositionEnum {
  LIST_PRICE = 'List Price',
  LIST_PRICE_AFTER = 'List Price After the following discounts',
  CUSTOMER_PRICE = 'Customer Price',
  CUSTOMER_PRICE_AFTER = 'Customer Price After the following discounts',
}
